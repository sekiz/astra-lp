---
phase: 02-static-page
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - dictionaries/en.json
  - dictionaries/tr.json
  - app/[lang]/page.tsx
  - app/[lang]/layout.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - PAGE-05

must_haves:
  truths:
    - "Visiting /en shows a full-viewport hero section with headline, subheadline, scarcity copy, and CTA button"
    - "Visiting /tr shows the same hero section with Turkish copy"
    - "The footer at /en and /tr contains a Privacy Policy link pointing to /en/privacy or /tr/privacy respectively"
    - "All Phase 2 dictionary keys in Hero, Features, WhyAstra, FinalCTA, and Footer sections are populated with non-empty strings in both en.json and tr.json"
    - "`npx vitest run` passes — dictionary non-empty tests are green"
    - "No `'use client'` directive is present in app/[lang]/page.tsx"
  artifacts:
    - path: "dictionaries/en.json"
      provides: "All Phase 2 copy in English — Hero, Features, WhyAstra, FinalCTA, Footer sections populated"
    - path: "dictionaries/tr.json"
      provides: "All Phase 2 copy in Turkish — parity with en.json, native-quality translation"
    - path: "app/[lang]/page.tsx"
      provides: "Hero section as async server component with getTranslations"
      contains: "section[data-section='hero']"
    - path: "app/[lang]/layout.tsx"
      provides: "Footer updated with privacy policy Link from i18n/navigation and dictionary-driven copyright"
      contains: "data-section='footer'"
  key_links:
    - from: "app/[lang]/page.tsx"
      to: "dictionaries/en.json Hero section"
      via: "getTranslations({ locale: lang, namespace: 'Hero' })"
      pattern: "getTranslations.*Hero"
    - from: "app/[lang]/layout.tsx"
      to: "dictionaries/en.json Footer section"
      via: "getTranslations({ locale: lang, namespace: 'Footer' })"
      pattern: "getTranslations.*Footer"
    - from: "app/[lang]/layout.tsx footer"
      to: "/privacy page"
      via: "Link from i18n/navigation"
      pattern: "Link.*href.*privacy"
---

<objective>
Populate both bilingual dictionaries with all Phase 2 copy, build the Hero section as a server component, and update the layout footer with the privacy policy link.

Purpose: Dictionaries must be populated before any component renders meaningful content. This plan delivers the "above the fold" experience — hero section visible on first load — and fulfills the footer privacy link requirement (PAGE-05). All copy is committed to dictionaries so the vitest non-empty assertions pass.
Output: Populated en.json + tr.json, Hero section in app/[lang]/page.tsx, updated footer in layout.tsx.
</objective>

<execution_context>
@/Users/sinan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sinan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-page/02-CONTEXT.md
@.planning/phases/02-static-page/02-RESEARCH.md
</context>

<interfaces>
<!-- Existing code contracts for executor -->

From app/[lang]/layout.tsx (current state):
- `LocaleLayout` is async server component with `params: Promise<{ lang: string }>`
- Already imports: `getTranslations`, `getMessages`, `NextIntlClientProvider`, `LanguageSwitcher`
- Footer currently: `<span className="text-xs text-[var(--color-text-muted)]">© 2026 Astra OS</span>` — hardcoded, no privacy link
- Import for locale-aware Link: `import { Link } from '../../i18n/navigation'`

From app/[lang]/page.tsx (current state):
- Minimal placeholder — just a `<main style={{ minHeight: '100vh' }}>{/* Phase 2 fills this in */}</main>`
- Props type: `{ params: Promise<{ lang: string }> }` — must be added
- Pattern to follow: `const { lang } = await params` then `const t = await getTranslations({ locale: lang, namespace: 'Hero' })`

From app/globals.css CSS tokens (confirmed in codebase):
- `--color-bg-primary`: page background (~#0a0a0a)
- `--color-bg-surface`: card background (#111111)
- `--color-bg-elevated`: elevated panel (#1a1a1a)
- `--color-text-primary`: main text (white-ish)
- `--color-text-secondary`: secondary text
- `--color-text-muted`: muted/tertiary text
- `--color-accent`: #6366f1 (indigo)
- `--color-accent-hover`: accent hover state
- `--color-border`: border color (#262626)

From i18n/navigation.ts:
- Exports locale-aware `Link` component — use `import { Link } from '../../i18n/navigation'` in layout.tsx
- Usage: `<Link href="/privacy">text</Link>` — automatically prepends locale

From lucide-react (install FIRST — see Task 1):
- Named imports: `import { ArrowRight } from 'lucide-react'`
- Usage: `<ArrowRight size={18} />`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and populate both dictionaries</name>
  <files>dictionaries/en.json, dictionaries/tr.json</files>
  <action>
**Step 1: Install lucide-react**

```bash
cd /Users/sinan/Dev/Astra/LP && npm install lucide-react
```

Verify it appears in `package.json` dependencies (not devDependencies — it's a runtime dependency).

**Step 2: Populate dictionaries/en.json**

Replace the entire file content. Keep all existing keys (Metadata, Nav, Privacy) unchanged — only fill in the empty-string values for Hero, Features, WhyAstra, FinalCTA, and Footer sections.

Write the following to `/Users/sinan/Dev/Astra/LP/dictionaries/en.json`:

```json
{
  "Metadata": {
    "title": "Astra OS — AI Operating System | Join the Closed Beta",
    "description": "Linux-based AI OS with kernel-level agent orchestration. First 100 developers shape the platform."
  },
  "Nav": {
    "switchLanguage": "TR",
    "switchLanguageLabel": "Switch to Turkish",
    "switchLanguageLabelAlt": "Switch to English"
  },
  "Privacy": {
    "title": "Privacy Policy",
    "intro": "Astra OS collects only the data you voluntarily provide during waitlist signup. We do not sell, rent, or share your data with third parties.",
    "dataCollectedTitle": "Data We Collect",
    "dataCollectedItems": "First name, last name, and email address (required)\nAnswers to 4 qualifying questions (role, AI agent usage, interests, motivation)\nBrowser locale and referral source (analytics only, not linked to your identity)",
    "retentionTitle": "Data Retention",
    "retentionBody": "Your data is retained for the duration of the closed-beta period. You may request deletion at any time by emailing the address below.",
    "rightsTitle": "Your Rights (GDPR / KVKK)",
    "rightsBody": "You have the right to access, correct, or delete your personal data. For KVKK rights (Turkish residents), the same rights apply under Turkish data protection law.",
    "contactTitle": "Contact",
    "contactBody": "For any privacy-related requests, email: privacy@astraos.com",
    "lastUpdated": "Last updated: March 2026"
  },
  "Hero": {
    "headline": "Your OS doesn't know what an AI agent is. Astra does.",
    "subheadline": "Astra OS is a Linux-based operating system with kernel-level AI agent orchestration — built for developers who are serious about what comes next.",
    "scarcity": "Closed beta. Limited to the first 100.",
    "ctaButton": "Get Early Access"
  },
  "Features": {
    "sectionTitle": "Built differently, from the ground up.",
    "card1Title": "Linux Kernel Foundation",
    "card1Body": "Built on a hardened Linux kernel — not a wrapper, not a compatibility layer. The same proven foundation that runs the world's most critical infrastructure, now optimized for AI workloads.",
    "card2Title": "Open Source, Always",
    "card2Body": "Every line of Astra OS that can be open will be. No black boxes, no lock-in. You can inspect the scheduler, audit the agent runtime, and fork whatever you need.",
    "card3Title": "Kernel-Level Agent Orchestration",
    "card3Body": "AI agent scheduling happens at the kernel, not in userspace middleware. Agents get real process isolation, direct hardware access, and first-class resource guarantees — not best-effort async glue."
  },
  "WhyAstra": {
    "sectionTitle": "Why not macOS or Ubuntu?",
    "para1": "macOS is built for humans using applications. The kernel scheduler does not know what a language model is, and Apple's security model treats AI agents as just another process — one it can silently throttle, sandbox, or kill. The result: agents lose context, pipelines break, and you spend your afternoon diagnosing permission errors instead of shipping.",
    "para2": "Ubuntu plus whatever AI framework is popular this month gets you further — but you are still duct-taping a general-purpose OS to a problem it was never designed for. Agent orchestration lives in Python libraries. Process isolation is an afterthought. Every agent run competes with your desktop environment for the same unmanaged thread pool.",
    "para3": "Astra OS starts from a different question: what if the OS knew agents were the primary workload? Kernel-level scheduling priorities for agent processes. First-class inter-agent communication via IPC primitives, not HTTP. Resource quotas enforced at the scheduler, not the application layer. This is not a framework — it is the operating system itself."
  },
  "FinalCTA": {
    "headline": "Ready to shape the future of AI infrastructure?",
    "scarcity": "Seats fill fast. We're reviewing applications now.",
    "ctaButton": "Join the Waitlist"
  },
  "Footer": {
    "privacyLink": "Privacy Policy",
    "switchLanguage": "TR",
    "switchLanguageLabel": "Switch to Turkish",
    "switchLanguageLabelAlt": "Switch to English",
    "copyright": "© 2026 Astra OS. All rights reserved."
  }
}
```

Note: WhyAstra uses `para1`, `para2`, `para3` sub-keys instead of a single `body` key. This is a schema change from the placeholder (which had a single `body` key). The new schema allows per-paragraph styling with `<strong>` tags in JSX without `dangerouslySetInnerHTML`. Update the key structure in BOTH JSON files consistently.

**Step 3: Populate dictionaries/tr.json**

Write the following to `/Users/sinan/Dev/Astra/LP/dictionaries/tr.json`:

```json
{
  "Metadata": {
    "title": "Astra OS — Yapay Zeka İşletim Sistemi | Kapalı Beta'ya Katıl",
    "description": "Çekirdek seviyesinde yapay zeka entegrasyonuna sahip Linux tabanlı işletim sistemi. İlk 100 geliştirici platformu şekillendiriyor."
  },
  "Nav": {
    "switchLanguage": "EN",
    "switchLanguageLabel": "Türkçe'ye geç",
    "switchLanguageLabelAlt": "İngilizce'ye geç"
  },
  "Privacy": {
    "title": "Gizlilik Politikası",
    "intro": "Astra OS, yalnızca bekleme listesi kaydı sırasında gönüllü olarak sağladığınız verileri toplar. Verilerinizi üçüncü taraflarla satmaz, kiralamaz veya paylaşmayız.",
    "dataCollectedTitle": "Topladığımız Veriler",
    "dataCollectedItems": "Ad, soyad ve e-posta adresi (zorunlu)\n4 yeterlilik sorusunun yanıtları (rol, yapay zeka ajan kullanımı, ilgi alanları, motivasyon)\nTarayıcı dili ve yönlendirme kaynağı (yalnızca analitik amaçlı, kimliğinizle ilişkilendirilmez)",
    "retentionTitle": "Veri Saklama",
    "retentionBody": "Verileriniz kapalı beta süresi boyunca saklanır. Aşağıdaki e-posta adresine yazarak istediğiniz zaman silinmesini talep edebilirsiniz.",
    "rightsTitle": "Haklarınız (GDPR / KVKK)",
    "rightsBody": "Kişisel verilerinize erişme, düzeltme veya silme hakkına sahipsiniz. KVKK kapsamındaki haklarınız (Türkiye'de ikamet edenler için) Türk veri koruma mevzuatı çerçevesinde geçerlidir.",
    "contactTitle": "İletişim",
    "contactBody": "Gizlilikle ilgili talepleriniz için e-posta gönderin: privacy@astraos.com",
    "lastUpdated": "Son güncelleme: Mart 2026"
  },
  "Hero": {
    "headline": "İşletim sisteminiz yapay zeka ajanının ne olduğunu bilmiyor. Astra biliyor.",
    "subheadline": "Astra OS, çekirdek seviyesinde yapay zeka ajan orkestrasyonuna sahip Linux tabanlı bir işletim sistemi — yapay zekanın geleceğini ciddiye alan geliştiriciler için.",
    "scarcity": "Kapalı beta. İlk 100 kullanıcıyla sınırlı.",
    "ctaButton": "Erken Erişim Al"
  },
  "Features": {
    "sectionTitle": "Temelden farklı inşa edildi.",
    "card1Title": "Linux Çekirdek Temeli",
    "card1Body": "Güçlendirilmiş bir Linux çekirdeği üzerine kurulu — katman değil, uyumluluk köprüsü değil. Dünyanın en kritik altyapısını çalıştıran kanıtlanmış temel, artık yapay zeka iş yükleri için optimize edildi.",
    "card2Title": "Her Zaman Açık Kaynak",
    "card2Body": "Astra OS'un açılabilecek her satırı açık olacak. Kara kutu yok, bağımlılık yok. Zamanlayıcıyı inceleyebilir, ajan çalışma ortamını denetleyebilir ve ihtiyaç duyduğunuz her şeyi fork edebilirsiniz.",
    "card3Title": "Çekirdek Seviyesinde Ajan Orkestrasyonu",
    "card3Body": "Yapay zeka ajan zamanlaması, userspace ara katmanında değil çekirdekte gerçekleşir. Ajanlar gerçek süreç izolasyonu, doğrudan donanım erişimi ve birinci sınıf kaynak garantileri alır — belirsiz async bant bezi değil."
  },
  "WhyAstra": {
    "sectionTitle": "Neden macOS veya Ubuntu değil?",
    "para1": "macOS, uygulama kullanan insanlar için tasarlandı. Çekirdek zamanlayıcısı bir dil modelinin ne olduğunu bilmiyor; Apple'ın güvenlik modeli yapay zeka ajanlarına sıradan bir süreç gibi davranıyor — sessizce kısıtlanabilir, sandbox'a alınabilir ya da sonlandırılabilir. Sonuç: ajanlar bağlamını kaybediyor, pipeline'lar bozuluyor, siz de öğleden sonranızı sevkiyat yapmak yerine izin hatalarını ayıklamakla geçiriyorsunuz.",
    "para2": "Ubuntu artı popüler yapay zeka framework'ü sizi daha ileriye taşıyor — ama hâlâ genel amaçlı bir işletim sistemini hiç tasarlanmadığı bir soruna bantlıyorsunuz. Ajan orkestrasyonu Python kütüphanelerinde yaşıyor. Süreç izolasyonu sonradan düşünülmüş. Her ajan çalıştırması, aynı yönetimsiz thread havuzu için masaüstü ortamınızla rekabet ediyor.",
    "para3": "Astra OS farklı bir soruyla başlıyor: ya işletim sistemi ajanların birincil iş yükü olduğunu bilseydi? Ajan süreçleri için çekirdek seviyesinde zamanlama öncelikleri. HTTP değil, IPC primitive'leri üzerinden birinci sınıf ajan iletişimi. Uygulama katmanında değil, zamanlayıcıda uygulanan kaynak kotaları. Bu bir framework değil — işletim sisteminin kendisi."
  },
  "FinalCTA": {
    "headline": "Yapay zeka altyapısının geleceğini şekillendirmeye hazır mısınız?",
    "scarcity": "Yerler hızlı dolduruluyor. Başvuruları şimdi inceliyoruz.",
    "ctaButton": "Bekleme Listesine Katıl"
  },
  "Footer": {
    "privacyLink": "Gizlilik Politikası",
    "switchLanguage": "EN",
    "switchLanguageLabel": "Türkçe'ye geç",
    "switchLanguageLabelAlt": "İngilizce'ye geç",
    "copyright": "© 2026 Astra OS. Tüm hakları saklıdır."
  }
}
```

**Verify dictionaries after writing:**
```bash
cd /Users/sinan/Dev/Astra/LP && npx vitest run tests/dictionaries.test.ts
```
Both parity tests AND the Phase 2 non-empty value tests must pass.
  </action>
  <verify>
    <automated>cd /Users/sinan/Dev/Astra/LP && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
`npx vitest run` exits 0. All dictionary tests pass: key parity + Phase 2 non-empty value assertions. `lucide-react` appears in package.json dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Hero section and update Footer in layout</name>
  <files>app/[lang]/page.tsx, app/[lang]/layout.tsx</files>
  <action>
**Part A: Hero section in app/[lang]/page.tsx**

Replace the placeholder file entirely. The page is an async server component. No `'use client'` directive. The CTA button is a plain `<button>` — no onClick, no `disabled` attribute (disabled causes visual opacity degradation). Leave a `TODO(Phase 3): wire to waitlist modal` comment on the button.

Write `/Users/sinan/Dev/Astra/LP/app/[lang]/page.tsx`:

```typescript
import { getTranslations } from 'next-intl/server'
import { ArrowRight } from 'lucide-react'

type Props = {
  params: Promise<{ lang: string }>
}

export default async function HomePage({ params }: Props) {
  const { lang } = await params
  const tHero = await getTranslations({ locale: lang, namespace: 'Hero' })

  return (
    <main>
      {/* ── Hero Section ──────────────────────────────────────────────── */}
      <section
        data-section="hero"
        className="relative flex flex-col items-center justify-center min-h-screen text-center px-6"
        style={{
          background:
            'radial-gradient(ellipse 60% 40% at 50% 40%, rgba(99,102,241,0.15) 0%, transparent 70%), var(--color-bg-primary)',
        }}
      >
        <h1 className="text-4xl md:text-6xl lg:text-7xl font-bold tracking-tight text-[var(--color-text-primary)] mb-6 max-w-4xl">
          {tHero('headline')}
        </h1>
        <p className="text-lg md:text-xl text-[var(--color-text-secondary)] max-w-2xl mb-5 leading-relaxed">
          {tHero('subheadline')}
        </p>
        <p className="text-sm text-[var(--color-text-muted)] mb-10 tracking-wide uppercase">
          {tHero('scarcity')}
        </p>
        {/* TODO(Phase 3): wire to waitlist modal — extract to CTAButton client component */}
        <button
          type="button"
          className="inline-flex items-center gap-2 px-7 py-3.5 rounded-lg bg-[var(--color-accent)] hover:bg-[var(--color-accent-hover)] text-white font-semibold text-base transition-colors cursor-pointer"
        >
          {tHero('ctaButton')}
          <ArrowRight size={18} />
        </button>
      </section>

      {/* Features, WhyAstra, FinalCTA sections added in Plans 03 and 04 */}
    </main>
  )
}
```

**Part B: Update Footer in app/[lang]/layout.tsx**

Modify the existing `LocaleLayout` function to:
1. Add `getTranslations` call for the `Footer` namespace (it already imports `getTranslations`)
2. Add `import { Link } from '../../i18n/navigation'` at top of file
3. Replace the hardcoded footer content with dictionary-driven content and a privacy link

The updated `LocaleLayout` function body (keep `generateMetadata` function unchanged):

```typescript
import type { Metadata } from 'next'
import { getTranslations, getMessages } from 'next-intl/server'
import { NextIntlClientProvider } from 'next-intl'
import { LanguageSwitcher } from '../../components/LanguageSwitcher'
import { Link } from '../../i18n/navigation'
import '../globals.css'

type Props = {
  children: React.ReactNode
  params: Promise<{ lang: string }>
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ lang: string }>
}): Promise<Metadata> {
  const { lang } = await params
  const t = await getTranslations({ locale: lang, namespace: 'Metadata' })

  return {
    metadataBase: new URL(process.env.NEXT_PUBLIC_BASE_URL ?? 'http://localhost:3000'),
    title: t('title'),
    description: t('description'),
    alternates: {
      canonical: `/${lang}`,
      languages: {
        en: '/en',
        tr: '/tr',
      },
    },
    openGraph: {
      type: 'website',
      locale: lang === 'tr' ? 'tr_TR' : 'en_US',
      title: t('title'),
      description: t('description'),
      images: [
        {
          url: '/og-image.png',
          width: 1200,
          height: 630,
          alt: t('title'),
        },
      ],
    },
    twitter: {
      card: 'summary_large_image',
      title: t('title'),
      description: t('description'),
    },
  }
}

export default async function LocaleLayout({ children, params }: Props) {
  const { lang } = await params
  const messages = await getMessages()
  const tFooter = await getTranslations({ locale: lang, namespace: 'Footer' })

  return (
    <html lang={lang}>
      <body>
        <NextIntlClientProvider messages={messages} locale={lang}>
          <header className="flex items-center justify-between px-6 py-4 border-b border-[var(--color-border)]">
            <span className="text-sm font-medium tracking-wider">ASTRA OS</span>
            <LanguageSwitcher />
          </header>

          {children}

          <footer
            data-section="footer"
            className="flex items-center justify-between px-6 py-4 border-t border-[var(--color-border)] mt-auto"
          >
            <Link
              href="/privacy"
              className="text-xs text-[var(--color-text-muted)] hover:text-[var(--color-text-secondary)] transition-colors"
            >
              {tFooter('privacyLink')}
            </Link>
            <span className="text-xs text-[var(--color-text-muted)]">
              {tFooter('copyright')}
            </span>
            <LanguageSwitcher />
          </footer>
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

After writing both files, run the dev server briefly to confirm there are no TypeScript errors:
```bash
cd /Users/sinan/Dev/Astra/LP && npx tsc --noEmit 2>&1 | head -20
```
  </action>
  <verify>
    <automated>cd /Users/sinan/Dev/Astra/LP && npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <done>
`npx tsc --noEmit` reports 0 errors. `app/[lang]/page.tsx` contains `data-section="hero"` with headline, subheadline, scarcity, and CTA button. `app/[lang]/layout.tsx` footer contains a `<Link href="/privacy">` component and dictionary-driven copyright. No `'use client'` in page.tsx.
  </done>
</task>

</tasks>

<verification>
Wave 1 complete when:
1. `npx vitest run` exits 0 — all dictionary tests green including Phase 2 non-empty assertions
2. `npx tsc --noEmit` exits 0
3. `npx playwright test tests/static-page.spec.ts --grep "hero"` tests pass (server running)
4. Footer privacy link test passes: `npx playwright test tests/static-page.spec.ts --grep "footer"`
</verification>

<success_criteria>
Dictionary copy is complete for both locales. Hero section renders on /en and /tr with full-viewport layout, radial gradient glow, headline, subheadline, scarcity, and CTA button. Footer contains a working locale-aware privacy link. Vitest passes. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-page/02-02-SUMMARY.md`
</output>
