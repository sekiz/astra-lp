---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - i18n/routing.ts
  - i18n/request.ts
  - middleware.ts
  - dictionaries/en.json
  - dictionaries/tr.json
  - app/[lang]/layout.tsx
autonomous: true
requirements:
  - I18N-01
  - I18N-03
  - I18N-04
  - I18N-05
  - SEO-01

must_haves:
  truths:
    - "GET / redirects to /en or /tr based on Accept-Language header (not hardcoded)"
    - "GET /en and GET /tr each render without 404 or redirect loop"
    - "Both /en and /tr page <head> contains hreflang alternates for the other locale"
    - "Both /en and /tr page <head> contains <title>, <meta description>, and og:type"
    - "dictionaries/en.json and dictionaries/tr.json exist with identical top-level key structure"
  artifacts:
    - path: "i18n/routing.ts"
      provides: "defineRouting config — locales ['en','tr'], localePrefix 'always', localeDetection true"
      exports: ["routing"]
    - path: "i18n/request.ts"
      provides: "getRequestConfig that loads locale dictionary"
      exports: ["default (getRequestConfig)"]
    - path: "middleware.ts"
      provides: "createMiddleware(routing) — locale detection, redirect, cookie, hreflang HTTP header"
      contains: "createMiddleware"
    - path: "dictionaries/en.json"
      provides: "English copy schema with all section keys; populated values for Metadata, Nav, Privacy"
    - path: "dictionaries/tr.json"
      provides: "Turkish copy schema — IDENTICAL keys to en.json; populated values for Metadata, Nav, Privacy"
    - path: "app/[lang]/layout.tsx"
      provides: "Root layout with generateMetadata (title, description, OG, hreflang alternates), html lang attr"
  key_links:
    - from: "middleware.ts"
      to: "i18n/routing.ts"
      via: "createMiddleware(routing)"
      pattern: "createMiddleware.*routing"
    - from: "i18n/request.ts"
      to: "dictionaries/en.json and dictionaries/tr.json"
      via: "dynamic import('../dictionaries/${locale}.json')"
      pattern: "import.*dictionaries.*locale"
    - from: "app/[lang]/layout.tsx"
      to: "i18n/routing.ts"
      via: "locale comes from params.lang; alternates.languages references both locales"
      pattern: "alternates.*languages"
---

<objective>
Wire next-intl v3 routing infrastructure: routing config, middleware, request config, dictionary schema, and root layout with full metadata. This is the architectural keystone — every component in Phase 2 will import from dictionaries and every URL relies on this routing config.

Purpose: Without this plan, the [lang] route segment does not exist, bare / returns a 404, and hreflang alternates are missing from the page head. All of Phase 2's bilingual components depend on the dictionary structure locked here.
Output: Working locale routing (/ → /en or /tr), populated dictionary schema for Phase 1 sections, root layout with SEO metadata.
</objective>

<execution_context>
@/Users/sinan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sinan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- next-intl v3 canonical patterns — use these exactly -->

i18n/routing.ts:
```typescript
import { defineRouting } from 'next-intl/routing'

export const routing = defineRouting({
  locales: ['en', 'tr'],
  defaultLocale: 'en',
  localePrefix: 'always',   // Both /en/ and /tr/ prefixed — locked user decision
  localeDetection: true     // Accept-Language header detection — locked user decision
})
```

middleware.ts:
```typescript
import createMiddleware from 'next-intl/middleware'
import { routing } from './i18n/routing'

export default createMiddleware(routing)

export const config = {
  matcher: '/((?!api|_next|_vercel|.*\\..*).*)'
}
```

i18n/request.ts:
```typescript
import { getRequestConfig } from 'next-intl/server'
import { routing } from './routing'

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale
  if (!locale || !routing.locales.includes(locale as 'en' | 'tr')) {
    locale = routing.defaultLocale
  }
  return {
    locale,
    messages: (await import(`../dictionaries/${locale}.json`)).default,
  }
})
```

app/[lang]/layout.tsx generateMetadata — uses NEXT_PUBLIC_BASE_URL env var, NOT hardcoded domain:
```typescript
metadataBase: new URL(process.env.NEXT_PUBLIC_BASE_URL ?? 'http://localhost:3000'),
```

Dictionary key schema (nested by section — discretionary choice, see RESEARCH.md Pattern 4):
All section keys must be present in BOTH en.json and tr.json even if empty objects {}.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create routing config, middleware, and request config</name>
  <files>i18n/routing.ts, i18n/request.ts, middleware.ts</files>
  <behavior>
    - GET / with no Accept-Language header → 307/308 redirect to /en/
    - GET / with Accept-Language: tr → 307/308 redirect to /tr/
    - GET /en → 200 (no redirect loop)
    - GET /tr → 200 (no redirect loop)
    - GET /some-unknown-path → redirect to /en/some-unknown-path or /tr/some-unknown-path
  </behavior>
  <action>
Create the `i18n/` directory and write three files.

**i18n/routing.ts**:
```typescript
import { defineRouting } from 'next-intl/routing'

export const routing = defineRouting({
  locales: ['en', 'tr'] as const,
  defaultLocale: 'en' as const,
  localePrefix: 'always',
  localeDetection: true,
})
```

**i18n/request.ts**:
```typescript
import { getRequestConfig } from 'next-intl/server'
import { routing } from './routing'

export default getRequestConfig(async ({ requestLocale }) => {
  let locale = await requestLocale
  if (!locale || !(routing.locales as readonly string[]).includes(locale)) {
    locale = routing.defaultLocale
  }
  return {
    locale,
    messages: (await import(`../dictionaries/${locale}.json`)).default,
  }
})
```

**middleware.ts** (project root — alongside next.config.ts, NOT inside app/):
```typescript
import createMiddleware from 'next-intl/middleware'
import { routing } from './i18n/routing'

export default createMiddleware(routing)

export const config = {
  // Match all pathnames except Next.js internals, API routes, and static files
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)'],
}
```

Important: middleware.ts must be at the project root, NOT inside the `app/` directory. Placing it inside app/ will cause it to be treated as a page, not a middleware.
  </action>
  <verify>
    <automated>cd /Users/sinan/Dev/Astra/LP && npx playwright test --grep "locale redirect" 2>&1 | tail -5</automated>
  </verify>
  <done>middleware.ts exists at project root. i18n/routing.ts and i18n/request.ts exist. `npx playwright test --grep "locale redirect"` passes (redirect tests green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create dictionary schema and root layout with metadata</name>
  <files>dictionaries/en.json, dictionaries/tr.json, app/[lang]/layout.tsx</files>
  <behavior>
    - GET /en page head contains <title> matching Metadata.title from en.json
    - GET /en page head contains <link rel="alternate" hreflang="tr">
    - GET /tr page head contains <link rel="alternate" hreflang="en">
    - GET /en page <html> has lang="en" attribute
    - GET /tr page <html> has lang="tr" attribute
    - npx vitest run tests/dictionaries.test.ts passes (tr.json keys match en.json keys)
  </behavior>
  <action>
**Step 1: Delete the existing app/layout.tsx** (scaffold-generated root layout). The locale-aware layout lives at `app/[lang]/layout.tsx`:
```bash
rm -f app/layout.tsx
```

**Step 2: Create app/[lang]/ directory structure**. Write `app/[lang]/layout.tsx`:

```typescript
import type { Metadata } from 'next'
import { getTranslations } from 'next-intl/server'
import { NextIntlClientProvider } from 'next-intl'
import { getMessages } from 'next-intl/server'
import '../globals.css'

type Props = {
  children: React.ReactNode
  params: Promise<{ lang: string }>
}

export async function generateMetadata({
  params,
}: {
  params: Promise<{ lang: string }>
}): Promise<Metadata> {
  const { lang } = await params
  const t = await getTranslations({ locale: lang, namespace: 'Metadata' })

  return {
    metadataBase: new URL(process.env.NEXT_PUBLIC_BASE_URL ?? 'http://localhost:3000'),
    title: t('title'),
    description: t('description'),
    alternates: {
      canonical: `/${lang}`,
      languages: {
        en: '/en',
        tr: '/tr',
      },
    },
    openGraph: {
      type: 'website',
      locale: lang === 'tr' ? 'tr_TR' : 'en_US',
      title: t('title'),
      description: t('description'),
      images: [
        {
          url: '/og-image.png',
          width: 1200,
          height: 630,
          alt: t('title'),
        },
      ],
    },
    twitter: {
      card: 'summary_large_image',
      title: t('title'),
      description: t('description'),
    },
  }
}

export default async function LocaleLayout({ children, params }: Props) {
  const { lang } = await params
  const messages = await getMessages()

  return (
    <html lang={lang}>
      <body>
        <NextIntlClientProvider messages={messages} locale={lang}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  )
}
```

**Step 3: Create dictionaries/en.json** — full key schema. Phase 1 sections (Metadata, Nav, Privacy) have real values. Phase 2+ sections (Hero, Features, WhyAstra, FinalCTA, Footer) have empty-string placeholder values — this establishes the schema contract for TypeScript key parity checking:

```json
{
  "Metadata": {
    "title": "Astra OS — AI Operating System | Join the Closed Beta",
    "description": "Linux-based AI OS with kernel-level agent orchestration. First 100 developers shape the platform."
  },
  "Nav": {
    "switchLanguage": "TR",
    "switchLanguageLabel": "Switch to Turkish",
    "switchLanguageLabelAlt": "Switch to English"
  },
  "Privacy": {
    "title": "Privacy Policy",
    "intro": "Astra OS collects only the data you voluntarily provide during waitlist signup. We do not sell, rent, or share your data with third parties.",
    "dataCollectedTitle": "Data We Collect",
    "dataCollectedItems": "First name, last name, and email address (required)\nAnswers to 4 qualifying questions (role, AI agent usage, interests, motivation)\nBrowser locale and referral source (analytics only, not linked to your identity)",
    "retentionTitle": "Data Retention",
    "retentionBody": "Your data is retained for the duration of the closed-beta period. You may request deletion at any time by emailing the address below.",
    "rightsTitle": "Your Rights (GDPR / KVKK)",
    "rightsBody": "You have the right to access, correct, or delete your personal data. For KVKK rights (Turkish residents), the same rights apply under Turkish data protection law.",
    "contactTitle": "Contact",
    "contactBody": "For any privacy-related requests, email: privacy@astraos.com",
    "lastUpdated": "Last updated: March 2026"
  },
  "Hero": {
    "headline": "",
    "subheadline": "",
    "scarcity": "",
    "ctaButton": ""
  },
  "Features": {
    "sectionTitle": "",
    "card1Title": "",
    "card1Body": "",
    "card2Title": "",
    "card2Body": "",
    "card3Title": "",
    "card3Body": ""
  },
  "WhyAstra": {
    "sectionTitle": "",
    "body": ""
  },
  "FinalCTA": {
    "headline": "",
    "scarcity": "",
    "ctaButton": ""
  },
  "Footer": {
    "privacyLink": "",
    "switchLanguage": "",
    "switchLanguageLabel": "",
    "switchLanguageLabelAlt": "",
    "copyright": ""
  }
}
```

**Step 4: Create dictionaries/tr.json** — IDENTICAL key structure to en.json. Phase 1 sections have native Turkish values. Phase 2+ sections have empty strings (matching en.json). Turkish copy for Metadata, Nav, and Privacy must be native-quality (not machine-translated):

```json
{
  "Metadata": {
    "title": "Astra OS — Yapay Zeka İşletim Sistemi | Kapalı Beta'ya Katıl",
    "description": "Çekirdek seviyesinde yapay zeka entegrasyonuna sahip Linux tabanlı işletim sistemi. İlk 100 geliştirici platformu şekillendiriyor."
  },
  "Nav": {
    "switchLanguage": "EN",
    "switchLanguageLabel": "Türkçe'ye geç",
    "switchLanguageLabelAlt": "İngilizce'ye geç"
  },
  "Privacy": {
    "title": "Gizlilik Politikası",
    "intro": "Astra OS, yalnızca bekleme listesi kaydı sırasında gönüllü olarak sağladığınız verileri toplar. Verilerinizi üçüncü taraflarla satmaz, kiralamaz veya paylaşmayız.",
    "dataCollectedTitle": "Topladığımız Veriler",
    "dataCollectedItems": "Ad, soyad ve e-posta adresi (zorunlu)\n4 yeterlilik sorusunun yanıtları (rol, yapay zeka ajan kullanımı, ilgi alanları, motivasyon)\nTarayıcı dili ve yönlendirme kaynağı (yalnızca analitik amaçlı, kimliğinizle ilişkilendirilmez)",
    "retentionTitle": "Veri Saklama",
    "retentionBody": "Verileriniz kapalı beta süresi boyunca saklanır. Aşağıdaki e-posta adresine yazarak istediğiniz zaman silinmesini talep edebilirsiniz.",
    "rightsTitle": "Haklarınız (GDPR / KVKK)",
    "rightsBody": "Kişisel verilerinize erişme, düzeltme veya silme hakkına sahipsiniz. KVKK kapsamındaki haklarınız (Türkiye'de ikamet edenler için) Türk veri koruma mevzuatı çerçevesinde geçerlidir.",
    "contactTitle": "İletişim",
    "contactBody": "Gizlilikle ilgili talepleriniz için e-posta gönderin: privacy@astraos.com",
    "lastUpdated": "Son güncelleme: Mart 2026"
  },
  "Hero": {
    "headline": "",
    "subheadline": "",
    "scarcity": "",
    "ctaButton": ""
  },
  "Features": {
    "sectionTitle": "",
    "card1Title": "",
    "card1Body": "",
    "card2Title": "",
    "card2Body": "",
    "card3Title": "",
    "card3Body": ""
  },
  "WhyAstra": {
    "sectionTitle": "",
    "body": ""
  },
  "FinalCTA": {
    "headline": "",
    "scarcity": "",
    "ctaButton": ""
  },
  "Footer": {
    "privacyLink": "",
    "switchLanguage": "",
    "switchLanguageLabel": "",
    "switchLanguageLabelAlt": "",
    "copyright": ""
  }
}
```

**Step 5: Create a minimal app/[lang]/page.tsx** placeholder so /en and /tr render a 200 (not 404). Phase 2 will replace this with real section components:

```typescript
// app/[lang]/page.tsx — placeholder; Phase 2 builds real content
import { useTranslations } from 'next-intl'

export default function HomePage() {
  const t = useTranslations('Hero')
  return (
    <main style={{ minHeight: '100vh' }}>
      {/* Phase 2 fills this in */}
    </main>
  )
}
```

**Step 6: Delete or clear** the scaffold-generated `app/page.tsx` if it still exists (conflicts with `app/[lang]/page.tsx`):
```bash
rm -f app/page.tsx
```

Note: If the scaffold created `app/layout.tsx` and it was not removed in Plan 02, remove it now — having both `app/layout.tsx` and `app/[lang]/layout.tsx` causes a layout conflict.
  </action>
  <verify>
    <automated>cd /Users/sinan/Dev/Astra/LP && npx playwright test --grep "hreflang" 2>&1 | tail -5 && npx vitest run tests/dictionaries.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Playwright hreflang tests pass. Vitest dictionary key parity test passes. `npx playwright test --grep "locale redirect"` passes. `npx playwright test --grep "SEO meta tags"` passes. Both /en and /tr return 200.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/sinan/Dev/Astra/LP
# Smoke suite covering this plan's requirements
npx playwright test --grep @smoke 2>&1 | tail -10
npx vitest run tests/dictionaries.test.ts 2>&1 | tail -5
```
All tests must pass or be in the expected "fail for missing content" state (empty Hero strings are fine — they won't cause test failures since those tests are in Phase 2).
</verification>

<success_criteria>
1. Visiting / redirects to /en or /tr based on Accept-Language (verified by Playwright)
2. Both /en and /tr render 200 with hreflang alternates in head (verified by Playwright)
3. en.json and tr.json have identical key structure (verified by Vitest)
4. Turkish copy for Metadata, Nav, Privacy is native-quality (not machine-translated placeholder)
5. No hardcoded copy strings exist in app/[lang]/layout.tsx or app/[lang]/page.tsx — all text comes from dictionaries
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
